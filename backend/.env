# Server Configuration
NODE_ENV=development
PORT=3001
BACKEND_URL=http://localhost:3001
FRONTEND_URL=http://localhost:5173

# Database (PostgreSQL)
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pilot_invoices
DB_USERNAME=mohannad.inairat
DB_PASSWORD=
DB_SSL=false

which node && node --version# n8n Configuration (optional for testing)
N8N_BASE_URL=http://localhost:5678
N8N_WEBHOOK_URL=http://localhost:5678/webhook/invoice-processing

# Security
LOG_LEVEL=info
JWT_SECRET=test_secret_for_development
